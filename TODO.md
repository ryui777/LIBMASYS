- [x] Update `api/config.php` local fallback allowed origins for localhost variants
- [x] Improve `js/log-sin.js` API helper error handling and user-facing catch messages
- [ ] Verify changes by testing login/register flow through browser URL in XAMPP
